'use strict'

const { typeOf } = require('../helpers/equal')

function diffObject (arr1, arr2, path) {

}

function diffEmptyObject (arr1, arr2, path) {

  if (a)
  // Type checking
  if (typeOf(a) !== typeOf(b)) {
    return false
  }
}

module.exports = {
  diffObject,
  diffEmptyObject
}
